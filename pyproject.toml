[project]
name = "bayes-alpha2"
version = "0.1.0"
description = ""
authors = [
    {name = "SwimelBee",email = "elias.bewitz@swimbird.se"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "yfinance (>=0.2.65,<0.3.0)",
    "scikit-learn (>=1.7.0,<2.0.0)",
    "colorama (>=0.4.6,<0.5.0)"
]

[tool.poetry]
packages = [{include = "bayes_alpha2", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
